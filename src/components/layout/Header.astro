---
const nav = [
  { label: "Directory", href: "/dispensaries/" },
  { label: "Map", href: "/map/" },
  { label: "Strains", href: "/strains/" },
  { label: "VT Guide", href: "/vermont-cannabis-guide/" },
  { label: "News", href: "/news/" },
];

const current = Astro.url.pathname;
---

<header class="bg-brand-ink text-white sticky top-0 z-50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-14">
      <a href="/" class="font-bold text-lg tracking-tight text-white hover:text-brand-orange transition-colors">
        VT Dispensary Directory
      </a>
      <nav class="hidden md:flex items-center gap-6">
        {nav.map(({ label, href }) => (
          <a
            href={href}
            class={`text-sm font-medium transition-colors hover:text-brand-orange ${
              current.startsWith(href) ? "text-brand-orange" : "text-gray-300"
            }`}
          >
            {label}
          </a>
        ))}
      </nav>
      <a href="/dispensaries/" class="btn-primary text-sm hidden md:inline-flex">
        Find a Dispensary
      </a>
    </div>
  </div>
</header>
